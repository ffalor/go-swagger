swagger: "2.0"
info:
  title: deprecated ops api
  version: "1.0.0"
paths:
  /old:
    get:
      operationId: getOldThing
      summary: Get the old thing
      deprecated: true
      responses:
        "200":
          description: Success
          schema:
            type: string
  /new:
    get:
      operationId: getNewThing
      summary: Get the new thing
      responses:
        "200":
          description: Success
          schema:
            type: string
